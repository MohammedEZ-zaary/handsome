{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_handsome_library",
            "type": "shell",
            "command": "./build.sh",
            "args": [],
            "group": {
                "kind":"build", 
                "isDefault": true 
            },
            "detail": "Build the library and the test project and run server via excuting build.sh bash"
        }
    ]
}
