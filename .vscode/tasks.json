{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_handsome_library",
            "type": "shell",
            "command": "python",
            "args": [
                "./build.py"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build the library and the test project and run server via executing build.sh bash"
        }
    ]
}
